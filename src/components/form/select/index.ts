import Root from './SelectRoot.vue';
import Button from './SelectButton.vue';
import Options from './SelectOptions.vue';
import Option from './SelectOption.vue';

export const Select = {
  Root,
  Button,
  Options,
  Option
};
